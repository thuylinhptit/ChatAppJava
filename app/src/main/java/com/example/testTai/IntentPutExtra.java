package com.example.testTai;

import android.os.Build;
import android.os.Parcel;
import android.os.Parcelable;

import androidx.annotation.RequiresApi;
//
//public class IntentPutExtra implements Parcelable {
////    private Object mData;
////
////    /* everything below here is for implementing Parcelable */
////
////    // 99.9% of the time you can just ignore this
////    @Override
////    public int describeContents() {
////        return 0;
////    }
////
////    // write your object's data to the passed-in Parcel
////    @Override
////    public void writeToParcel(Parcel out, int flags) {
////        out.writeTypedObject(mData, flags);
////    }
////
////    // this is used to regenerate your object. All Parcelables must have a CREATOR that implements these two methods
////    public static final Parcelable.Creator<IntentPutExtra> CREATOR = new Parcelable.Creator<IntentPutExtra>() {
////        public IntentPutExtra createFromParcel(Parcel in) {
////            return new IntentPutExtra(in);
////        }
////
////        public IntentPutExtra[] newArray(int size) {
////            return new IntentPutExtra[size];
////        }
////    };
////
////    // example constructor that takes a Parcel and gives you an object populated with it's values
////    private IntentPutExtra(Parcel in) {
////        mData = in.readTypedObject(Object);
////    }
//}
